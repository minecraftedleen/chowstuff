const words=["Hello", "world", "this", "is", "web", "development"]
const filtered = fruits.filter(function(fruit){
    if (fruit.substring(0,1) === "C") {
        return fruit;
    }
})
console.log(filtered)

